import React from 'react';

const SearchBar = () => {
  return (
      <div>
        <input type="text" placeholder="This is the SearchBar" />
        <button>Click here...</button>
      </div>
  );
};

export default SearchBar;